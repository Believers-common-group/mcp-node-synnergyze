name: p4-economic-model-adapter-test

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]
    paths:
      - 'api/adapters/**'
      - '.github/workflows/p4-economic-model-adapter-test.yml'

jobs:
  economic-model-adapter:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Install dependencies
        run: npm i

      - name: Run focused economic model adapter tests
        run: npx vitest run api/adapters/economic-model-adapter.test.ts

      - name: Type-check API runtime and adapters
        run: npm run -s type-check
