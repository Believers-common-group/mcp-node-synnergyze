name: archived-live-neon-persistence-verify

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Archived verification workflow. No automatic execution.'
