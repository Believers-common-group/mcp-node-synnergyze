{
  "name": "@believers-common/alpha-bnr-db-gate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "npm run cf-typegen && tsc --noEmit",
    "check": "wrangler check",
    "dry-run": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.5",
    "typescript": "^5.8.3",
    "wrangler": "latest"
  }
}
